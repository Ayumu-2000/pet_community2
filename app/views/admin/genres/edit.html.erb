<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <h4>ジャンル編集</h4>
      <%= form_with model: @genre, url: admin_genre_path, method: :patch do |f| %>
        <h4>ジャンル名</h4>
        <%= f.text_field :genre_name %><br>
        <%= f.submit "更新する" %>
      <% end %>
    </div>
  </div>
</div>